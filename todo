WASM API for modifying existing specializations, adding new specializations, and removing specializations, we want to be able to override functionality but be able to call the default implementation so people can just add one thing to something and keep the rest if they want to
add WebAuthn/security-key authentication as both passwordless login (when enabled) and 2FA for password logins
